[build-system]
requires=["setuptools>=69","wheel"]
build-backend="setuptools.build_meta"

[project]
name="zortex-mt8168-recovery-suite"
version="1.0.0"
description="Read-only Android restoration validation platform"
requires-python=">=3.11"

dependencies=[
"click",
"rich",
"pydantic",
]

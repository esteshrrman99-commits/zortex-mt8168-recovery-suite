# ZORTEX v1.0 Analysis Pipeline

Run the complete read-only workflow with:

```bash
bash scripts/zortex_all.sh
```

The pipeline generates reports, runs tests, and hashes artifacts.
It does not flash, unlock, erase, or modify a device.
